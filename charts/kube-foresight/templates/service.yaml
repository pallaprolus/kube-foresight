apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-foresight.fullname" . }}
  labels:
    {{- include "kube-foresight.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-foresight.selectorLabels" . | nindent 4 }}
